# Eris BLE split keyboard — shield definitions
# No whitespace after the comma or in the part name!

config SHIELD_ERIS_LEFT
    def_bool $(shields_list_contains,eris_left)

config SHIELD_ERIS_RIGHT
    def_bool $(shields_list_contains,eris_right)
